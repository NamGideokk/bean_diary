import 'package:flutter/material.dart';

class FrequentlyAskedQuestionsMain extends StatelessWidget {
  const FrequentlyAskedQuestionsMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("자주 묻는 질문"),
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
